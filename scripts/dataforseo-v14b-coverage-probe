#!/usr/bin/env bash
set -euo pipefail
PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$PROJECT_ROOT"
PYTHONPATH=src exec python3 -c 'from amazon_scout.dataforseo_v14b_coverage import main; raise SystemExit(main())'
