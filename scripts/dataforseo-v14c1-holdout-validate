#!/usr/bin/env bash
set -euo pipefail
PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$PROJECT_ROOT"
PYTHONPATH=src exec python3 -c 'from amazon_scout.holdout_validation_v14c1 import main; raise SystemExit(main())'
